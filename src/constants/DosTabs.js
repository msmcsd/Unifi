// This corresponds to DosTab enum in TestTask.cs
export const DosTab = Object.freeze({
	Test: 0, // This needs to be the first tab since this is the default tab if not probidfed in json
	Dev: 1,
	AppInventory: 2,
	Web: 3,
	Rollback: 4,
});
